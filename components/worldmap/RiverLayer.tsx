/**
 * Layer: Rivers & lakes.
 *
 * Not implemented yet — needs a hydrography dataset (e.g. Natural Earth
 * rivers/lakes, 50m or 10m) that isn't part of the current project assets.
 * Kept as an explicit module for the same reason as TerrainLayer.
 */
export default function RiverLayer() {
  return null;
}
