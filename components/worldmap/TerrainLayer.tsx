/**
 * Layer: Terrain (mountains, forests, plains, deserts, elevation shading).
 *
 * Not implemented yet — this needs a resolution/elevation dataset (e.g.
 * Natural Earth raster relief, or a vector biome/elevation layer) that
 * isn't part of the current project assets. Kept as an explicit module so
 * it slots into <EchelonWorldMap> without restructuring the layer stack
 * once that data is available.
 */
export default function TerrainLayer() {
  return null;
}
